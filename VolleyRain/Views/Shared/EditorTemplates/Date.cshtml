@model DateTime?

@{
    var metadata = ModelMetadata.FromLambdaExpression(model => model, ViewData);
}

@Html.TextBoxFor(model => model, metadata.EditFormatString, new { @class = ViewData["class"] })